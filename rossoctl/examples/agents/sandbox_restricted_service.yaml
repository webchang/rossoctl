apiVersion: v1
kind: Service
metadata:
  name: sandbox-restricted
  namespace: team1
  labels:
    rossoctl.io/type: agent
    app.kubernetes.io/name: sandbox-restricted
spec:
  selector:
    rossoctl.io/type: agent
    app.kubernetes.io/name: sandbox-restricted
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http

apiVersion: v1
kind: Service
metadata:
  name: sandbox-legion
  namespace: team1
  labels:
    rossoctl.io/type: agent
    app.kubernetes.io/name: sandbox-legion
spec:
  selector:
    rossoctl.io/type: agent
    app.kubernetes.io/name: sandbox-legion
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http

# Service manifest for weather-service agent
# Companion to weather_service_deployment.yaml
apiVersion: v1
kind: Service
metadata:
  name: weather-service
  namespace: team1
  labels:
    rossoctl.io/type: agent
    protocol.rossoctl.io/a2a: ""
    app.kubernetes.io/name: weather-service
    app.kubernetes.io/managed-by: rossoctl-e2e
spec:
  type: ClusterIP
  selector:
    rossoctl.io/type: agent
    app.kubernetes.io/name: weather-service
  ports:
  - name: http
    port: 8080
    targetPort: 8000
    protocol: TCP

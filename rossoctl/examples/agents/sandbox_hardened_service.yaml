apiVersion: v1
kind: Service
metadata:
  name: sandbox-hardened
  namespace: team1
  labels:
    rossoctl.io/type: agent
    app.kubernetes.io/name: sandbox-hardened
spec:
  selector:
    rossoctl.io/type: agent
    app.kubernetes.io/name: sandbox-hardened
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http

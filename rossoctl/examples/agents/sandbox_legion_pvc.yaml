# Shared RWX PVC for sandbox-legion context workspaces
# StorageClass must support ReadWriteMany:
#   Kind: nfs
#   OpenShift ODF: ocs-storagecluster-cephfs
#   AWS EFS: efs-sc
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sandbox-legion-workspace
  namespace: team1
  labels:
    rossoctl.io/type: agent-workspace
    rossoctl.io/agent: sandbox-legion
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ocs-storagecluster-cephfs
  resources:
    requests:
      storage: 5Gi

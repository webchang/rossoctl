apiVersion: v1
kind: Service
metadata:
  name: sandbox-agent
  namespace: team1
  labels:
    rossoctl.io/type: agent
    app.kubernetes.io/name: sandbox-agent
spec:
  selector:
    rossoctl.io/type: agent
    app.kubernetes.io/name: sandbox-agent
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http

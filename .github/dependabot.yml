version: 2
updates:
  # GitHub Actions — keep workflow actions up to date
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    labels:
      - dependencies
    groups:
      minor-and-patch:
        update-types: [minor, patch]
      major:
        update-types: [major]

  # Python (root) — E2E test dependencies
  - package-ecosystem: pip
    directory: /
    schedule:
      interval: weekly
    labels:
      - dependencies
    groups:
      minor-and-patch:
        update-types: [minor, patch]
      major:
        update-types: [major]

  # Python (backend) — FastAPI backend dependencies
  - package-ecosystem: pip
    directory: /rossoctl/backend
    schedule:
      interval: weekly
    labels:
      - dependencies
    groups:
      minor-and-patch:
        update-types: [minor, patch]
      major:
        update-types: [major]
    ignore:
      # cryptography 47+ crashes with SIGILL on ARM64 Kind clusters.
      # Pin is intentional — the pyproject cap is cryptography<47.
      # `versions: [">=47"]` blocks BOTH semver-major version bumps AND
      # requirement-range-widening PRs (e.g. "Update cryptography requirement
      # from <47 to <50"), which `update-types` alone does not catch.
      # Effect: only minor+patch bumps below 47 allowed while the crash reproduces.
      # Root cause (pyca/cryptography#14764, closed): not a library bug but stale
      # ARM64 emulation in old Docker Desktop (<=4.38). The pin guards CI/dev runners
      # still on those versions.
      # TODO: drop this ignore (and the pyproject <47 cap) once all runners are on a
      # Docker that no longer SIGILLs — verify by importing cryptography 47+ on ARM64 Kind.
      - dependency-name: "cryptography"
        versions: [">=47"]

  # npm (UI) — React frontend dependencies
  - package-ecosystem: npm
    directory: /rossoctl/ui-v2
    schedule:
      interval: weekly
    labels:
      - dependencies
    groups:
      minor-and-patch:
        update-types: [minor, patch]
      major:
        update-types: [major]
    ignore:
      # keycloak-js 26.x requires Web Crypto API (crypto.randomUUID, crypto.subtle)
      # which is unavailable over plain HTTP. Block until all environments use HTTPS.
      - dependency-name: "keycloak-js"
        update-types: [version-update:semver-major]
      # PatternFly packages must be upgraded together (PF5→PF6).
      # Block major version bumps to prevent partial upgrades.
      - dependency-name: "@patternfly/*"
        update-types: [version-update:semver-major]
      # eslint-plugin-react-refresh 0.5.x requires ESLint 9+.
      # Block until ESLint 8→9 migration is done (see #905).
      - dependency-name: "eslint-plugin-react-refresh"
        versions: [">=0.5.0"]
      # typescript-eslint parser and plugin must be upgraded together.
      # Block until ESLint 8→9 migration is done (see #905).
      - dependency-name: "@typescript-eslint/parser"
        update-types: [version-update:semver-major]
      - dependency-name: "@typescript-eslint/eslint-plugin"
        update-types: [version-update:semver-major]
      # ESLint 9+/10+ requires flat config and all plugins upgraded together.
      # Block until ESLint 8→9 migration is done (see #905).
      - dependency-name: "eslint"
        update-types: [version-update:semver-major]
      - dependency-name: "eslint-plugin-react-hooks"
        update-types: [version-update:semver-major]

  # Go (TUI) — Go module dependencies
  - package-ecosystem: gomod
    directory: /rossoctl/tui
    schedule:
      interval: weekly
    labels:
      - dependencies
    groups:
      minor-and-patch:
        update-types: [minor, patch]
      major:
        update-types: [major]

  # Docker — CI support images
  - package-ecosystem: docker
    directory: /.github/dockerfiles/hypershift-ci
    schedule:
      interval: weekly
    labels:
      - dependencies
    groups:
      minor-and-patch:
        update-types: [minor, patch]
      major:
        update-types: [major]

  # Docker — application images
  - package-ecosystem: docker
    directory: /rossoctl/backend
    schedule:
      interval: weekly
    labels:
      - dependencies
    groups:
      minor-and-patch:
        update-types: [minor, patch]
      major:
        update-types: [major]

  - package-ecosystem: docker
    directory: /rossoctl/ui-v2
    schedule:
      interval: weekly
    labels:
      - dependencies
    groups:
      minor-and-patch:
        update-types: [minor, patch]
      major:
        update-types: [major]

  - package-ecosystem: docker
    directory: /rossoctl/auth/agent-oauth-secret
    schedule:
      interval: weekly
    labels:
      - dependencies
    groups:
      minor-and-patch:
        update-types: [minor, patch]
      major:
        update-types: [major]

  - package-ecosystem: docker
    directory: /rossoctl/auth/api-oauth-secret
    schedule:
      interval: weekly
    labels:
      - dependencies
    groups:
      minor-and-patch:
        update-types: [minor, patch]
      major:
        update-types: [major]

  - package-ecosystem: docker
    directory: /rossoctl/auth/mlflow-oauth-secret
    schedule:
      interval: weekly
    labels:
      - dependencies
    groups:
      minor-and-patch:
        update-types: [minor, patch]
      major:
        update-types: [major]

  - package-ecosystem: docker
    directory: /rossoctl/auth/ui-oauth-secret
    schedule:
      interval: weekly
    labels:
      - dependencies
    groups:
      minor-and-patch:
        update-types: [minor, patch]
      major:
        update-types: [major]

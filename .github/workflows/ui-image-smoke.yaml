name: UI Image Smoke

# Builds the rossoctl-ui image and actually boots nginx (`nginx -t`). Manifest-only
# tests (helm unittest/template) never start the container, so they cannot catch
# nginx config errors — e.g. a duplicate worker_processes directive, which is what
# regressed the rossoctl#1991 fix. Scoped to the ui-v2 image sources so unrelated
# PRs don't pay for an image build.
on:
  push:
    branches: [ "main" ]
    paths:
      - 'rossoctl/ui-v2/**'
      - '.github/workflows/ui-image-smoke.yaml'
  pull_request:
    branches: [ "main" ]
    paths:
      - 'rossoctl/ui-v2/**'
      - '.github/workflows/ui-image-smoke.yaml'

# Explicit permissions - principle of least privilege
permissions:
  contents: read

jobs:
  ui-image-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e  # v4.3.0

      - name: Build rossoctl-ui image (load locally, no push)
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a  # v7.3.0
        with:
          context: ./rossoctl
          file: ./rossoctl/ui-v2/Dockerfile
          push: false
          load: true
          tags: rossoctl-ui-v2:ci-smoke

      - name: Smoke test - nginx config is valid and workers are pinned (rossoctl#1991)
        run: |
          set -euo pipefail
          # `nginx -t` runs the base-image entrypoint then validates the full
          # config; a duplicate/invalid directive fails here with [emerg].
          # nginx resolves proxy_pass upstreams at config-test time, so stub the
          # backend host (there is no rossoctl-backend Service in CI). A duplicate
          # worker_processes still fails earlier, at main-conf parse time.
          run_nginx() { docker run --rm --add-host rossoctl-backend:127.0.0.1 rossoctl-ui-v2:ci-smoke "$@"; }
          run_nginx nginx -t
          # Dump the effective config and assert worker_processes is a fixed
          # count, not the host-CPU `auto` that OOMKilled the pod on large nodes.
          workers=$(run_nginx nginx -T 2>/dev/null \
            | awk '/^worker_processes/{gsub(/;/,"",$2); print $2; exit}')
          echo "effective worker_processes = '${workers}'"
          case "${workers}" in
            ''|auto)
              echo "::error::nginx worker_processes must be pinned to a fixed count (got '${workers}'). See rossoctl#1991."
              exit 1
              ;;
          esac

apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: sandbox-legion-platform
  namespace: team1
spec:
  source:
    type: Git
    git:
      uri: https://github.com/Ladas/rossoctl.git
      ref: feat/platform-agent-runtime
    contextDir: deployments/sandbox
    sourceSecret:
      name: github-shipwright-secret
  strategy:
    type: Docker
    dockerStrategy:
      dockerfilePath: agents/legion/Dockerfile.combined
      noCache: true
  output:
    to:
      kind: ImageStreamTag
      name: sandbox-legion-platform:v0.0.1
  runPolicy: Serial
---
apiVersion: image.openshift.io/v1
kind: ImageStream
metadata:
  name: sandbox-legion-platform
  namespace: team1

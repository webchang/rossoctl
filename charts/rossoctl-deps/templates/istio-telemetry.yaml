{{- if and .Values.components.otel.enabled (not .Values.openshift) (.Capabilities.APIVersions.Has "telemetry.istio.io/v1") }}
apiVersion: telemetry.istio.io/v1
kind: Telemetry
metadata:
  name: mesh-tracing
  namespace: {{ .Values.istio.namespace | default "istio-system" }}
  labels:
    {{- include "rossoctl.labels" . | nindent 4 }}
spec:
  tracing:
  - providers:
    - name: otel-tracing
    randomSamplingPercentage: {{ .Values.istio.tracing.randomSamplingPercentage }}
{{- end }}

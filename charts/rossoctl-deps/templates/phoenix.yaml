{{- if .Values.components.phoenix.enabled }}
{{- if not .Values.components.otel.enabled }}
{{- fail "components.phoenix.enabled requires components.otel.enabled (Phoenix depends on the OTEL Collector)" }}
{{- end }}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: phoenix
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "rossoctl.labels" . | nindent 4 }}
spec:
  serviceName: phoenix
  replicas: 1
  selector:
    matchLabels:
      app: phoenix
  template:
    metadata:
      annotations:
        prometheus.io/path: /metrics
        prometheus.io/port: "9090"
        prometheus.io/scrape: "true"
      labels:
        app: phoenix
    spec:
      containers:
      - args:
        - -m
        - phoenix.server.main
        - serve
        command:
        - python
        env:
        {{- if eq ((.Values.phoenix).database).type "sqlite" }}
        - name: PHOENIX_SQL_DATABASE_URL
          value: "sqlite:///{{ (.Values.phoenix.database.sqlite).path | default "/mnt/data/phoenix.db" }}"
        - name: PHOENIX_WORKING_DIR
          value: /mnt/data
        - name: PHOENIX_PORT
          value: "6006"
        - name: PHOENIX_GRPC_PORT
          value: "4317"
        {{- else }}
        - name: PHOENIX_POSTGRES_HOST
          value: postgres
        - name: PHOENIX_POSTGRES_PORT
          value: "5432"
        - name: PHOENIX_POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: otel-db-secret
              key: username
        - name: PHOENIX_POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: otel-db-secret
              key: password
        - name: PHOENIX_POSTGRES_DB
          value: postgres
        - name: PHOENIX_SQL_DATABASE_POOL_PRE_PING
          value: "true"
        - name: PHOENIX_SQL_DATABASE_POOL_SIZE
          value: "5"
        - name: PHOENIX_SQL_DATABASE_MAX_OVERFLOW
          value: "10"
        - name: PHOENIX_SQL_DATABASE_POOL_RECYCLE
          value: "3600"
        - name: PHOENIX_WORKING_DIR
          value: /mnt/data
        - name: PHOENIX_PORT
          value: "6006"
        {{- end }}
        {{- with .Values.phoenix.extraEnvVars }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        image: {{ .Values.phoenix.image.repository }}:{{ .Values.phoenix.image.tag }}
        name: phoenix
        ports:
        - containerPort: 6006
        - containerPort: 9090
        - containerPort: 4317
        readinessProbe:
          httpGet:
            port: 6006
        volumeMounts:
        - mountPath: /mnt/data
          name: phoenix
  volumeClaimTemplates:
  - metadata:
      name: phoenix
    spec:
      accessModes:
      - ReadWriteOnce
      resources:
        requests:
          storage: 8Gi
---
apiVersion: v1
kind: Service
metadata:
  name: phoenix
  namespace: {{ .Release.Namespace }}
  labels:
    app: phoenix
    app.kubernetes.io/instance: phoenix
    app.kubernetes.io/name: phoenix
    {{- include "rossoctl.labels" . | nindent 4 }}
spec:
  ports:
  - appProtocol: http
    name: http
    port: 6006
    protocol: TCP
    targetPort: 6006
  - appProtocol: http
    name: http2
    port: 9090
    protocol: TCP
    targetPort: 9090
  - appProtocol: grpc
    name: grpc
    port: 4317
    protocol: TCP
    targetPort: 4317
  selector:
    app: phoenix
  type: ClusterIP
---
{{- if .Values.openshift }}
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: phoenix
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "rossoctl.labels" . | nindent 4 }}
  annotations:
    openshift.io/host.generated: "true"
spec:
  path: /
  port:
    targetPort: 6006
  to:
    kind: Service
    name: phoenix
  wildcardPolicy: None
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
{{- else }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: phoenix
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "rossoctl.labels" . | nindent 4 }}
    app: phoenix
spec:
  parentRefs:
    - name: http
      namespace: {{ .Release.Namespace }}
  hostnames:
    - phoenix.{{ .Values.domain | default "localtest.me" }}
  rules:
    - backendRefs:
        - name: phoenix
          port: 6006

{{- end }}
{{- end }}
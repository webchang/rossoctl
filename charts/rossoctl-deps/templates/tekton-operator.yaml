{{- if and .Values.components.tekton.enabled .Values.openshift }}
{{- /* Skip if Tekton/Pipelines operator Subscription already exists */}}
{{- if not (lookup "operators.coreos.com/v1alpha1" "Subscription" "openshift-operators" "openshift-pipelines-operator-rh") }}
apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: openshift-pipelines-operator-rh
  namespace: openshift-operators
  labels:
    operators.coreos.com/openshift-pipelines-operator-rh.openshift-operators: ""
    {{- include "rossoctl.labels" . | nindent 4 }}
spec:
  channel: latest
  installPlanApproval: Automatic
  name: openshift-pipelines-operator-rh
  source: redhat-operators
  sourceNamespace: openshift-marketplace
{{- end }}
{{- end }}

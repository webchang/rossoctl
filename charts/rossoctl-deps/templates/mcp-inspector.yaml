{{- if .Values.components.mcpInspector.enabled  }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "rossoctl.fullname" . }}-mcp-inspector-sa
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "rossoctl.labels" . | nindent 4 }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "rossoctl.fullname" . }}-mcp-inspector-role
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "rossoctl.labels" . | nindent 4 }}
rules:
{{- if .Values.openshift }}
- apiGroups: ["route.openshift.io"]
  resources: ["routes"]
  verbs: ["get", "list", "watch"]
{{- else }}
- apiGroups: ["networking.k8s.io"]
  resources: ["ingresses"]
  verbs: ["get", "list", "watch"]
- apiGroups: ["gateway.networking.k8s.io"]
  resources: ["httproutes"]
  verbs: ["get", "list", "watch"]
{{- end }}
- apiGroups: [""]
  resources: ["configmaps"]
  verbs: ["get", "create", "update", "patch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name:  {{ include "rossoctl.fullname" . }}-mcp-inspector-rolebinding
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "rossoctl.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "rossoctl.fullname" . }}-mcp-inspector-role
subjects:
  - kind: ServiceAccount
    name: {{ include "rossoctl.fullname" . }}-mcp-inspector-sa
---
# nginx reverse proxy so the Inspector UI (6274) and its proxy server (6277)
# are reachable on a SINGLE origin: "/" -> UI, "/mcp-proxy/" -> proxy server
# (prefix-stripped). This lets the browser use one already-trusted cert/host
# instead of a separate mcp-proxy Route whose self-signed cert is never
# accepted (only hit via background fetch).
apiVersion: v1
kind: ConfigMap
metadata:
  name: mcp-inspector-nginx
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "rossoctl.labels" . | nindent 4 }}
data:
  nginx.conf: |
    worker_processes 1;
    # Run under OpenShift's restricted SCC (arbitrary non-root UID): keep the
    # pid and all temp paths in writable /tmp instead of /var/cache/nginx.
    pid /tmp/nginx.pid;
    events { worker_connections 1024; }
    http {
      access_log /dev/stdout;
      error_log  /dev/stderr;
      client_body_temp_path /tmp/nginx-client;
      proxy_temp_path       /tmp/nginx-proxy;
      fastcgi_temp_path     /tmp/nginx-fastcgi;
      uwsgi_temp_path       /tmp/nginx-uwsgi;
      scgi_temp_path        /tmp/nginx-scgi;
      # Preserve WebSocket/Upgrade semantics through the proxy.
      map $http_upgrade $connection_upgrade { default upgrade; '' close; }
      server {
        listen 8080;
        proxy_http_version 1.1;
        proxy_set_header Host              $host;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade           $http_upgrade;
        proxy_set_header Connection        $connection_upgrade;

        # MCP Inspector proxy server (streamable-http / SSE). The trailing
        # slash on proxy_pass strips the /mcp-proxy prefix so the proxy sees
        # /health, /mcp, /sse, etc. SSE needs buffering off + long timeouts.
        location /mcp-proxy/ {
          proxy_pass http://127.0.0.1:6277/;
          proxy_buffering off;
          proxy_cache off;
          proxy_read_timeout 3600s;
          proxy_send_timeout 3600s;
        }
        location = /mcp-proxy {
          proxy_pass http://127.0.0.1:6277/;
          proxy_buffering off;
          proxy_cache off;
          proxy_read_timeout 3600s;
          proxy_send_timeout 3600s;
        }

        # MCP Inspector UI.
        location / {
          proxy_pass http://127.0.0.1:6274;
          proxy_read_timeout 3600s;
        }
      }
    }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-inspector
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "rossoctl.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mcp-inspector
  template:
    metadata:
      labels:
        app: mcp-inspector
    spec:
      serviceAccountName: {{ include "rossoctl.fullname" . }}-mcp-inspector-sa
      initContainers:
      - name: route-checker-and-config-creator
        image: "{{ .Values.common.kubectlImage }}"
        command:
          - "/bin/sh"
          - "-c"
          - |
            set -e
            echo "Starting MCP config job..."

            {{- if .Values.openshift }}
            # Function to wait for a specific route to be admitted
            wait_for_route() {
              ROUTE_NAME=$1
              NAMESPACE=$2
              echo "Waiting for route '$ROUTE_NAME' in namespace '$NAMESPACE' to be admitted..."
              until [ "$(kubectl get route $ROUTE_NAME -n $NAMESPACE -o jsonpath='{.status.ingress[0].conditions[?(@.type=="Admitted")].status}' 2>/dev/null)" = "True" ]; do
                echo "Route '$ROUTE_NAME' not admitted yet. Retrying in 15 seconds..."
                sleep 15
              done
              echo "Route '$ROUTE_NAME' is admitted."
            }

            # Wait for route
            wait_for_route mcp-inspector {{ .Release.Namespace }}

            echo "Route is ready. Fetching hostnames..."

              # Get hostnames from the route
            INSPECTOR_HOST=$(kubectl get route mcp-inspector -n {{ .Release.Namespace }} -o jsonpath='{.spec.host}')
            PROTOCOL=https
            # Set PORT to an empty string for OpenShift to use the default https port (443)
            PORT=""
            {{- else }}
            # Get hostname from the HTTPRoute hostnames
            PROTOCOL=http
            PORT=8080
            INSPECTOR_HOST=$(kubectl get httproute mcp-inspector -n {{ .Release.Namespace }} -o jsonpath='{.spec.hostnames[0]}')

            # If no hostname in HTTPRoute, construct from domain
            if [ -z "$INSPECTOR_HOST" ]; then
              INSPECTOR_HOST="mcp-inspector.{{ .Values.domain }}"
            fi
            {{- end }}

            if [ -z "$INSPECTOR_HOST" ]; then
              echo "Error: Could not retrieve hostname."
              exit 1
            fi

            echo "Inspector host: $INSPECTOR_HOST"

            # Create the ConfigMap using a heredoc and apply it
            echo "Creating/updating mcp-inspector-config ConfigMap..."
            kubectl apply -n {{ .Release.Namespace }} -f - <<EOF
            apiVersion: v1
            kind: ConfigMap
            metadata:
              name: mcp-inspector-config
            data:
              # Conditionally include the port if the PORT variable is not empty
              ALLOWED_ORIGINS: "${PROTOCOL}://${INSPECTOR_HOST}{{ if .Values.openshift | not }}:${PORT}{{ end }}"
            EOF

            echo "ConfigMap 'mcp-inspector-config' applied successfully."
      containers:
        - name: mcp-inspector
          image: "{{ .Values.mcpInspector.image }}:{{ .Values.mcpInspector.tag }}"
          ports:
            - containerPort: 6274
            - containerPort: 6277
          resources:
            limits:
              cpu: 100m
              memory: 256Mi
            requests:
              cpu: 25m
              memory: 64Mi
          env:
          - name: DANGEROUSLY_OMIT_AUTH
            value: "true"
          - name: ALLOWED_ORIGINS
            valueFrom:
              configMapKeyRef:
                name: mcp-inspector-config
                key: ALLOWED_ORIGINS
          - name: HOST
            value: 0.0.0.0
        - name: nginx
          image: "{{ .Values.mcpInspector.nginxImage | default "mirror.gcr.io/nginx:1.27-alpine" }}"
          ports:
            - containerPort: 8080
          resources:
            limits:
              cpu: 50m
              memory: 64Mi
            requests:
              cpu: 10m
              memory: 32Mi
          volumeMounts:
            - name: nginx-config
              mountPath: /etc/nginx/nginx.conf
              subPath: nginx.conf
      volumes:
        - name: nginx-config
          configMap:
            name: mcp-inspector-nginx
---
apiVersion: v1
kind: Service
metadata:
  name: mcp-inspector
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "rossoctl.labels" . | nindent 4 }}
spec:
  selector:
    app: mcp-inspector
  ports:
  # Single entrypoint: nginx sidecar fronts both the UI (6274) and the proxy
  # server (6277) so everything is served from one host.
  - name: http
    port: 8080
    protocol: TCP
    targetPort: 8080
  type: ClusterIP
---
{{- if .Values.openshift }}
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  annotations:
    openshift.io/host.generated: "true"
    # The Inspector proxy uses a long-lived SSE stream (streamable-http MCP
    # transport). HAProxy's default route timeout (~30s) would drop it, so
    # extend it.
    haproxy.router.openshift.io/timeout: {{ .Values.mcpInspector.routeTimeout | default "1h" | quote }}
  name: mcp-inspector
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "rossoctl.labels" . | nindent 4 }}
spec:
  path: /
  port:
    targetPort: http
  to:
    kind: Service
    name: mcp-inspector
  wildcardPolicy: None
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
{{- else }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: mcp-inspector
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "rossoctl.labels" . | nindent 4 }}
spec:
  parentRefs:
  - name: {{ .Values.httpGateway.name | default "http" }}
    sectionName: {{ .Values.httpGateway.sectionName | default "http" }}
    namespace: {{ .Values.httpGateway.namespace | default "rossoctl-system" }}
  hostnames:
  - mcp-inspector.{{ .Values.domain | default "localtest.me" }}
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: mcp-inspector
      port: 8080 # nginx sidecar fronts both UI and proxy
{{- end }}
{{- end }}

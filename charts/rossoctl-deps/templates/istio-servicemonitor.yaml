{{- if and .Values.components.istio.enabled .Values.openshift }}
apiVersion: v1
kind: Service
metadata:
  name: ztunnel-service
  namespace: "{{ .Values.istio.ztunnel.namespace }}"
  labels:
    app: ztunnel
    {{- include "rossoctl.labels" . | nindent 4 }}
spec:
  selector:
    app: ztunnel
  ports:
  - name: ztunnel-stats
    port: 15020
    targetPort: 15020
{{- if and (.Capabilities.APIVersions.Has "monitoring.coreos.com/v1") .Values.components.kiali.enabled }}
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ztunnel-servicemonitor
  namespace: "{{ .Values.istio.ztunnel.namespace }}"
  labels:
    release: prometheus
    {{- include "rossoctl.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      app: ztunnel
  namespaceSelector:
    matchNames:
    - istio-ztunnel
  endpoints:
  - port: ztunnel-stats
    interval: 10s
    path: /metrics
{{- end }}
{{- end }}